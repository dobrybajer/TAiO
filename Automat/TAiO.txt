1. AutomataGenerator - tworzenie losowego automatu (pocz¹tkowe wartoœci), w ka¿dej kolumnie dok³adnie jedna 1, rozmiar macierzy (i,j,k), gdzie:
- i-wiersze macierzy danego stanu 
- j-kolumny macierzy danego stanu
- k-wymiar odpowiadaj¹cy liczbie podzia³ów cech

INPUT: liczba symboli (s_cnt), liczba podzia³u przedzia³u (0,1) dla cech (d_cnt)
OUTPUT: macierz przejœcia (3D) danego automatu (M), zawieraj¹ca tyle macierzy (2D) ile wynosi d_cnt

2. AutomataComputation - Symulacja obliczenia automatu, poprzez mno¿enie macierzy (zamiast mno¿enia -> min, zamiast sumowania -> max) 

INPUT: macierz przejœcia danego automatu (t_mtx), s³owo wejœciowe reprezentowane przez symbole z podzia³u cech (w_input)
OUTPUT: numer wykrytego symbolu (n)

3. FileReader - zaczytanie wartoœci z pliku tekstowego do programu. 

*  Wartoœci dziesiêtne oddzielone kropk¹!
** Obs³uguje zaczytywanie tylko wartoœci numerycznych, jeœli na wejœciu bêdzie znak, nast¹pi b³¹d.

INPUT: nazwa pliku (filename), separator (dlm)
OUTPUT: wektor wszystkich symboli wejœciowych z pliku (s_list), iloœæ reprezentantów klasy (c_cnt), macierz cech (a_mtx)

4. FileWriter - zaczytanie wartoœci z macierzy do pliku tekstowego.

*  Wartoœci dziesiêtne oddzielone kropk¹!
** Obs³uguje zapisywanie tylko wartoœci numerycznych

INPUT: nazwa pliku (filename), macierz z wartoœciami (mtx), separator (dlm)
OUTPUT: brak

5. LearningSetGenerator - Tworzenie (losowanie) zbioru ucz¹cego oraz zapisanie go do pliku

INPUT: liczba symboli (s_cnt), liczba reprezentantów klasy danego symbolu (c_cnt), liczba cech (a_cnt), liczba podzia³ów przedzia³u (0,1) na cyfry 1,2,...,n (d_cnt), dolne ograniczenie dla przedzia³u rozkladu jednostajnego (l_bnd), górne ograniczenie dla przedzia³u rozk³adu jednostajnego (u_bnd)
OUTPUT: wygenerowany zbiór ucz¹cy (l_set)

6. ErrorFunction - Funkcja b³êdu symuluj¹ca obliczenie automatu dla danego zbioru wejœciowego (ucz¹cego) oraz danej macierzy przejœcia automatu.
Funkcja zwraca liczbê b³êdów oraz u³amek udanych rozpoznañ.

INPUT: nazwa pliku wejœciowego (fname), macierz przejœcia automatu (mtx)
OUTPUT: liczba znalezionych b³êdów (e_cnt), u³amek b³êdnych rozpoznañ (e_proc)

7. Main - skrypt uruchamiaj¹cy ca³e zadanie dla przyk³adowych wartoœci. Wykonuje nastêpuj¹ce kroki:
a) Ustawienie odpowiednich parametrów
b) Wygenerowanie zbioru ucz¹cego (jako macierz, oraz do pliku)
c) Wygenerowanie pocz¹tkowego automatu (losowy)
d) Wykonanie funkcji b³êdu dla zbioru ucz¹cego oraz wygenerowanego automatu, wyœwietlenie iloœci b³êdnie rozpoznanych znaków oraz u³amka b³êdnie rozpoznanych znaków
e) TODO: u¿ycie PSO (tak naprawdê mo¿na po³¹czyæ z d))
f) TODO: u¿ycie funkcji b³êdu dla konkretnego zbioru treningowego oraz wygenerowanego z PSO automatu.

8. PSO - In progress...